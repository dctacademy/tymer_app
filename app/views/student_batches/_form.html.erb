<%= form_for(@student_batch) do |f| %>
  <% if @student_batch.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@student_batch.errors.count, "error") %> prohibited this student_batch from being saved:</h2>

      <ul>
      <% @student_batch.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
    
    <div class="form-group">
      <%= f.label :student_id %>
      <%= f.text_field :student_id, value: Student.find(@student_batch.student_id).name, disabled: true, class: "form-control" %>
    </div>

    <div class="form-group">
    <%= f.label :batch_id %>
    <%= f.text_field :batch_id, value: Batch.find(@student_batch.batch_id).title, disabled: true, class: "form-control" %>
    </div>
   <div class="form-group">
      <%= f.label :course_fee %><br>
      <%= f.text_field :course_fee, class: "form-control" %>
   </div>
    <%= f.submit "Submit", class: "btn btn-primary" %>
    <% end %>
