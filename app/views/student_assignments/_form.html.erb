<%= form_for(@student_assignment) do |f| %>
  <% if @student_assignment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@student_assignment.errors.count, "error") %> prohibited this student_assignment from being saved:</h2>

      <ul>
      <% @student_assignment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :student_id %><br>
    <%= f.number_field :student_id %>
  </div>
  <div class="field">
    <%= f.label :batch_assignment_id %><br>
    <%= f.number_field :batch_assignment_id %>
  </div>
  <div class="field">
    <%= f.label :submitted_on %><br>
    <%= f.datetime_select :submitted_on %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
