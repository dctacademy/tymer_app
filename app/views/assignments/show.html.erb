<div class="col-md-8">
  <div class="page-header">
    <h2><%= @assignment.name %></h2>
  </div>

  <div class="well">
		<p><%= raw(@assignment.description) %></p>

	<b><%= link_to @assignment.chapter.try(:name), chapter_path(@assignment.chapter_id) %> , <%= link_to @assignment.topic.try(:name), topic_path(@assignment.topic_id) %></b>
  </div>
	


<%= link_to 'Edit', edit_assignment_path(@assignment) %> |
<%= link_to 'Back', assignments_path %>

</div>
