<%= form_for(@job_source) do |f| %>
  <% if @job_source.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@job_source.errors.count, "error") %> prohibited this job source from being saved:</h2>

      <ul>
      <% @job_source.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  
  <div class="form-group">
    <%= f.label :name %><br>
    <%= f.text_field :name, class: "form-control" %>
  </div>

  <div class="actions">
    <%= f.submit "Submit", class: "btn btn-default" %> or <%= link_to 'Back', job_sources_path %>
  </div>
<% end %>
